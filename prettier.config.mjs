/** @type {import("prettier").Config} */

const config = {
    trailingComma: 'es5',
    useTabs: false,
    semi: true,
    singleQuote: true,
    tabWidth: 4,
    printWidth: 128,
    parser: 'flow',
};

export default config;
