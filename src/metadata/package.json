{
  "name": "speaker-metadata-manager",
  "version": "1.0.0",
  "description": "Web-based interface for managing speaker metadata in the Spinorama project",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
